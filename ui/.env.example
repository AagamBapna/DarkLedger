# Agentic Shadow-Cap — Dashboard Configuration
# Copy to .env and adjust values as needed.

# Base URL for the Daml JSON API proxy (Nginx)
VITE_LEDGER_API_URL=http://localhost:7575

# Polling interval for contract queries (milliseconds)
VITE_POLL_INTERVAL_MS=3000
